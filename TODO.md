# TODO

- [x] Add SD web UI assets:
  - [x] `ui/login.html`
  - [x] `ui/dashboard.html`

- [ ] Add `/data` folder assets (if needed)
- [x] Update ESP32 code to write logs to `/data/security_log.txt` (instead of root `/security_log.txt`)

- [ ] Copy `ui/` and `data/` folders to SD card
- [ ] Flash Arduino sketch and test login + dashboard

